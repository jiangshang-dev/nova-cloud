# =========================================================
# 仅保留：应用名 / Profile 组合 / Nacos
# 网关无数据源；配置按 common / security 拆分
# =========================================================
spring:
  application:
    name: nova-gateway
  profiles:
    group:
      dev:
        - common
        - security
    active: ${SPRING_PROFILES_ACTIVE:dev}
  cloud:
    nacos:
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
      server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
      discovery:
        enabled: true
        namespace: ${NACOS_NAMESPACE:nova-cloud}
        group: DEFAULT_GROUP
      config:
        enabled: true
        namespace: ${NACOS_NAMESPACE:nova-cloud}
        group: DEFAULT_GROUP
        file-extension: yml
        refresh-enabled: true
  config:
    import:
      - optional:nacos:${spring.application.name}-dev.${spring.cloud.nacos.config.file-extension}
      - optional:nacos:application-common.yml
      - optional:nacos:application-security.yml
