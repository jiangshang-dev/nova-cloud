集成 Gateway、OAuth2.1、Nacos、Sentinel、SkyWalking、
Redis、RocketMQ/RabbitMQ、Elasticsearch、XXL-JOB、
Prometheus、Grafana、国密 SM2/SM4、多数据库兼容、多对象存储、
大文件分片上传、分布式事务及 K8s 部署能力。

兼容数据库在配置文件中可以切换配置文件，每个数据库都对应的配置文件：
Database
├── MySQL
├── PostgreSQL
├── Oracle
└── Dameng

数据库配置文件、公共配置文件、按业务场景区分一下不同配置文件

platform:
    datasource:
        db-type: mysql


这样以后甚至可以扩展，现在加todo留空：
SQL Server
人大金仓
OceanBase
TiDB
GaussDB