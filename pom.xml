<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.9</version>
        <relativePath/>
    </parent>

    <groupId>com.nova</groupId>
    <artifactId>nova-cloud</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>NovaCloud</name>
    <description>NovaCloud：企业级云原生微服务与智能体开发平台</description>

    <modules>
        <module>nova-common</module>
        <module>nova-starters</module>
        <module>nova-platform</module>
        <module>nova-business</module>
        <module>nova-search</module>
        <module>nova-nacos</module>
        <module>nova-ai</module>
        <module>nova-visual</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Spring Cloud -->
        <spring-cloud.version>2024.0.3</spring-cloud.version>
        <spring-cloud-alibaba.version>2023.0.3.4</spring-cloud-alibaba.version>

        <!-- ORM / Cache / Utils -->
        <mybatis-plus.version>3.5.12</mybatis-plus.version>
        <shardingsphere.version>5.5.0</shardingsphere.version>
        <dynamic-datasource.version>4.3.1</dynamic-datasource.version>
        <druid.version>1.2.24</druid.version>
        <hutool.version>5.8.36</hutool.version>
        <redisson.version>3.45.1</redisson.version>
        <!-- API Docs -->
        <springdoc.version>2.8.9</springdoc.version>
        <!-- MQ -->
        <rocketmq-spring.version>2.3.1</rocketmq-spring.version>
        <!-- AI Agent -->
        <agentscope.version>2.0.1</agentscope.version>
        <!-- Nova internal -->
        <nova.version>1.0-SNAPSHOT</nova.version>
        <!-- AWS -->
        <aws.version>2.40.15</aws.version>
        <!-- Spring Boot Admin（对齐 Boot 3.4） -->
        <spring-boot-admin.version>3.4.7</spring-boot-admin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud Alibaba BOM（Nacos / Sentinel / RocketMQ / Seata） -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ========== Nova Common ========== -->
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-common-core</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-common-redis</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-common-security</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-common-log</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-common-crypto</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-common-ai</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <!-- ========== Nova Starters ========== -->
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-ai-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-crypto-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-demo-api</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-demo-biz</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-file-api</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-file-biz</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-file-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-mq-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-datasource-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-mybatis-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>nova-shardingsphere-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>sentinel-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nova</groupId>
                <artifactId>skywalking-spring-boot-starter</artifactId>
                <version>${nova.version}</version>
            </dependency>

            <!-- Spring Boot Admin -->
            <dependency>
                <groupId>de.codecentric</groupId>
                <artifactId>spring-boot-admin-starter-server</artifactId>
                <version>${spring-boot-admin.version}</version>
            </dependency>
            <dependency>
                <groupId>de.codecentric</groupId>
                <artifactId>spring-boot-admin-starter-client</artifactId>
                <version>${spring-boot-admin.version}</version>
            </dependency>

            <!-- MyBatis-Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- ShardingSphere JDBC（可选：dynamic 里挂 sharding-db） -->
            <dependency>
                <groupId>org.apache.shardingsphere</groupId>
                <artifactId>shardingsphere-jdbc</artifactId>
                <version>${shardingsphere.version}</version>
            </dependency>
            <!-- 动态数据源（对齐 Jeecg：spring.datasource.dynamic） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>dynamic-datasource-spring-boot3-starter</artifactId>
                <version>${dynamic-datasource.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- Hutool -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <!-- Redisson -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <!-- SpringDoc / OpenAPI -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- RocketMQ（显式声明，便于业务直接引用；SCA BOM 中也有管理） -->
            <dependency>
                <groupId>org.apache.rocketmq</groupId>
                <artifactId>rocketmq-spring-boot-starter</artifactId>
                <version>${rocketmq-spring.version}</version>
            </dependency>

            <!-- AgentScope 2 Java -->
            <dependency>
                <groupId>io.agentscope</groupId>
                <artifactId>agentscope-harness</artifactId>
                <version>${agentscope.version}</version>
            </dependency>
            <dependency>
                <groupId>io.agentscope</groupId>
                <artifactId>agentscope-core</artifactId>
                <version>${agentscope.version}</version>
            </dependency>
            <dependency>
                <groupId>io.agentscope</groupId>
                <artifactId>agentscope-extensions-model-dashscope</artifactId>
                <version>${agentscope.version}</version>
            </dependency>
            <dependency>
                <groupId>io.agentscope</groupId>
                <artifactId>agentscope-extensions-model-openai</artifactId>
                <version>${agentscope.version}</version>
            </dependency>
            <dependency>
                <groupId>io.agentscope</groupId>
                <artifactId>agentscope-extensions-model-ollama</artifactId>
                <version>${agentscope.version}</version>
            </dependency>
            <dependency>
                <groupId>io.agentscope</groupId>
                <artifactId>agentscope-extensions-redis</artifactId>
                <version>${agentscope.version}</version>
            </dependency>

            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${aws.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <!-- 父工程与 common 无主类，避免 spring-boot:run 误跑 -->
                        <skip>true</skip>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
    </repositories>
</project>
